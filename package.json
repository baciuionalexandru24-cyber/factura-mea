{
  "name": "verifica-factura",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "import:news": "node scripts/import-news-drafts.cjs",
    "schedule:approved": "node scripts/schedule-approved-content.cjs",
    "publish:scheduled": "node scripts/publish-scheduled-content.cjs",
    "validate:content": "node scripts/validate-editorial-content.cjs",
    "automate:editorial": "npm run import:news && npm run schedule:approved && npm run publish:scheduled",
    "generate:seo": "npm run schedule:approved && npm run publish:scheduled && npm run validate:content && node scripts/generate-content-assets.cjs",
    "generate:social": "npm run validate:content && node scripts/generate-social-assets.cjs",
    "generate:all": "npm run schedule:approved && npm run publish:scheduled && npm run validate:content && node scripts/generate-content-assets.cjs && node scripts/generate-social-assets.cjs",
    "prebuild": "npm run generate:all",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
